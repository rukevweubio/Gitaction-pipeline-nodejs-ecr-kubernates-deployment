{
  "name": "nodejs-express-ecr-app",
  "version": "0.1.0",
  "description": "Simple Express app (serves index.html) with tests and CI/CD to push Docker image to AWS ECR",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand --detectOpenHandles"
  },
  "engines": {
    "node": "^18 || ^20"
  },
  "author": "rukevwe ubioworo",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "nodemon": "^3.1.10",
    "supertest": "^6.3.3"
  }
}
