apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-nodejs-app.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "my-nodejs-app.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
